<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gaurav.travelexpensemanager">

    <application
        android:allowBackup="true"
        android:icon="@drawable/unnamed"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UserActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddTripDetailsActivity" />
        <activity android:name=".ExpenseDetailsActivity" />
        <activity android:name=".TripTableActivity" />
        <activity android:name=".BalancedBudgetActivity" />
        <activity android:name=".ExpenseTableActivity" />
        <activity android:name=".TripWiseExpenseActivity" />
        <activity android:name=".CategoryWiseExpensesActivity" />
        <activity android:name=".DayWiseExpenseActivity"></activity>
    </application>

</manifest>